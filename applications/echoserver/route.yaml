---
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: echoserver
spec:
  parentRef:
    group: gateway.networking.k8s.io	
    kind: Gateway
    name: envoy-proxy
    namespace: backbone
  listeners:
  - name: foo
    hostname: "*.echoserver.jynolen.fr"
    protocol: HTTP
    port: 80
  - name: https
    protocol: HTTPS
    hostname: "*.echoserver.jynolen.fr"
    port: 443
    allowedRoutes:
      namespaces:
        from: All
    tls:
      mode: Terminate
      certificateRefs:
      - name: echoserver-jynolen-tls
        group: ''	
        kind: Secret
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: echoserver
spec:
  parentRefs:
    - group: gateway.networking.k8s.io	
      kind: ListenerSet
      name: echoserver
  hostnames:
    - "testouile.echoserver.jynolen.fr"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: echoserver
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
# ---
# apiVersion: gateway.envoyproxy.io/v1alpha1
# kind: SecurityPolicy
# metadata:
#   name: echoserver
# spec:
#   targetRefs:
#     - group: gateway.networking.k8s.io
#       kind: HTTPRoute
#       name: echoserver
#   oidc:
#     provider:
#       issuer: "https://keycloak.jynolen.fr/realms/kubernetes"
#     clientID: "echoserver"
#     clientSecret:
#       name: "echoserver-oidc-secret"
#     redirectURL: "https://echo.jynolen.fr/oauth2/callback"
#     logoutPath: "/logout"