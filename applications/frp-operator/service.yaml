apiVersion: v1
kind: Service
metadata:
  name: frp-controller-manager-metrics-service
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: https
  selector:
    app.kubernetes.io/name: frp-controller-manager
    control-plane: controller-manager