apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
spec:
  internalTrafficPolicy: Cluster
  ports:
    - name: psql
      port: 5432
      protocol: TCP
      targetPort: psql
  sessionAffinity: None
  type: ClusterIP
